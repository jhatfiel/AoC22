import { AoCPuzzle } from "../../lib/AoCPuzzle";

export class a201815 extends AoCPuzzle {
    sampleMode(): void { }

    _loadData(lines: string[]) {
    }

    _runStep(): boolean {
        this.result = "Result";
        return false;
    }
}