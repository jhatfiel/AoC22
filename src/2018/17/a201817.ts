import { AoCPuzzle } from '../../lib/AoCPuzzle';

export class a201817 extends AoCPuzzle {
    sampleMode(): void { };

    _loadData(lines: string[]) { }

    _runStep(): boolean {
        let moreToDo = false;
        this.result = 'Result';
        return moreToDo;
    }
}